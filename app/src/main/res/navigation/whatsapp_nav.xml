<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/whatsapp_nav"
    app:startDestination="@id/whatsappFragment">

    <fragment
        android:id="@+id/whatsappFragment"
        android:name="com.whatsappstatus.WhatsappFragment"
        tools:layout="@layout/fragment_whatsapp">

    </fragment>

    <fragment
        android:id="@+id/statusFragment"
        android:name="com.whatsappstatus.StatusFragment"
        android:label="fragment_status"
        tools:layout="@layout/fragment_status" />
</navigation>